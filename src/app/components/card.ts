import { Component, OnInit, output } from "@angular/core";
import { interval, timeout, timer } from "rxjs";

@Component({
    selector: 'yt-card',
    template: `
        Панель
    `,
})
export class Card {

}